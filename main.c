#include<stdio.h>
#include <unistd.h>
# include "question1.h"


int main() {
    // 1. Affichage d’un message d’accueil, suivi d’un prompt simple. Par exemple :

    afficher_message_accueil();
    // 2. Exécution de la commande saisie et retour au prompt (REPL : read–eval–print loop) :
    char commande[100];
    ssize_t taille_buffer;

    while (1)
    {
        /* code */
    }
    
    return 0;
}